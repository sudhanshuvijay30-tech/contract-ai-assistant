services:
  contract-ai-assistant:
    build: .
    container_name: contract-ai-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

